import React from "react";
import { Text } from "@chakra-ui/react";

const Home = (): React.ReactElement => {
  return (
    <div style={{ textAlign: "center", paddingTop: "20px" }}>
      <Text textStyle="heading">Homepage 🏠</Text>
    </div>
  );
};

export default Home;
